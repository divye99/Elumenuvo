import { SkHubShell } from "@/components/storefront/Skeleton";
/** Brand hub skeleton: sidebar + three rails, same 1360/230px geometry. */
export default function BrandLoading() { return <SkHubShell rails={3} />; }
