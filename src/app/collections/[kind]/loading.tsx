import { SkHubShell } from "@/components/storefront/Skeleton";
/** Collection skeleton: sidebar + per-category rails, same geometry. */
export default function CollectionLoading() { return <SkHubShell rails={4} />; }
