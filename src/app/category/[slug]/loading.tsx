import { SkHubShell } from "@/components/storefront/Skeleton";
/** Category hub skeleton: sidebar + three rails, same 1360/230px geometry. */
export default function CategoryLoading() { return <SkHubShell rails={3} />; }
